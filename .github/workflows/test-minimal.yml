name: Minimal Test

on:
  push:
    branches: [ feature/test-pr ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World"
      - run: echo "Test successful"